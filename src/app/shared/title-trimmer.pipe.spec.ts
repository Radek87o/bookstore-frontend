import { TitleTrimmerPipe } from './title-trimmer.pipe';

describe('TitleTrimmerPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleTrimmerPipe();
    expect(pipe).toBeTruthy();
  });
});
