import { PriceDisplayPipe } from './price-display.pipe';

describe('PriceDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
